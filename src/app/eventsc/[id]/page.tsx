// import { EventHeaderInfo } from "@/components/ui/event-header-info/EventHeaderInfo";

// interface Props {
//   params: Promise<{ id: number }>
// }

// export default async function Event({ params }: Props) {
//   const { id } = await params;

//   return (
//     <EventHeaderInfo key={ id } eventId={ id } />
//   );
// }
